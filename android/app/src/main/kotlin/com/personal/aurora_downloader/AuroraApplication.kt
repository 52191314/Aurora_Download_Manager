package com.personal.aurora_downloader

import android.app.Application

/**
 * Application entry.
 *
 * OSS edition: Play Feature Delivery (SplitCompat) was removed with the
 * Play Core dependency — fat APK builds ship every native library, so no
 * split-compat is needed.
 */
class AuroraApplication : Application()
